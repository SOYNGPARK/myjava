package myjava;

public class VariableTest {
	int count; // 객체변수 = 인스턴스 변수
	static int amount; // 클래스 변수 (전역변수라는 말은 쓰지 않음)
	public static void main(String[] args) {
		int count; // 지역변수 = local variable = 함수 내부에서 선언, 활용
	}

}
